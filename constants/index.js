"use strict";

const cost_types = require("./cost_types");
const dose_types = require("./dose_types");
const vaccine_types = require("./vaccine_types");

module.exports = {
    CostType : cost_types,
    DoseType : dose_types,
    VaccineType : vaccine_types
}
